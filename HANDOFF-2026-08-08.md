# HANDOFF — 2026-08-08

Este archivo complementa `WORKSPACE_STATUS.md`, no lo reemplaza. Léelo primero a él —
está muy completo (427 líneas, cada fase documentada, verificación en producción incluida,
bloque `NEXT_SESSION_BOOTSTRAP` al final). Esto solo cubre lo que la sesión anterior
**no alcanzó a registrar** porque se cortó por límite de contexto.

## Por qué existe este archivo

La sesión anterior (título "Landing de inscripción y pago HYBRID", en Claude Code
Desktop) terminó abruptamente con `Prompt is too long` justo después de cerrar y
publicar la fase `PLANB-CLIP-PAYMENT-01 — Frente A`. El usuario alcanzó a archivar esa
sesión pero no a copiar su contenido antes de que se llenara el contexto. Este handoff
se reconstruyó desde el transcript de esa sesión archivada (`ccd_session_mgmt`) más
verificación directa del repo, en otra sesión (`hybrid-event-landing`), a pedido del
usuario.

## Estado verificado ahora mismo (2026-08-08)

- **HEAD local:** `b21ad87` — `docs(workspace): correct acta — Frente A is deployed, not local-only`
- **`origin/main`:** también en `b21ad87` — sincronizado, 0 commits de diferencia en ambos sentidos
- **Árbol:** limpio
- Esto coincide exactamente con lo que ya declara `WORKSPACE_STATUS.md` → confía en su
  contenido, no está desactualizado.

## Lo único que falta: la pregunta que cortó la sesión

El último turno de la conversación anterior, **sin responder**, fue (texto literal del
usuario):

> "tenemos que volver a hacer la pausa, tenemos la anterior lámina anterior que
> pausamos? solo cambiaremos la fecha, no hagas nada solo dime si la tienes"

Interpretación: el usuario quiere **reactivar temporalmente el sitio en modo pausa**
(la misma "lámina"/pantalla de espera que ya se usó una vez — ver `Fase HOLDING-PAGE-01`
en `WORKSPACE_STATUS.md`, línea ~224), **cambiando solo la fecha** que se muestra, no
rehaciendo el mecanismo desde cero.

**El mecanismo ya existe y sigue en el repo, apagado:**

- `src/config/holdingMode.ts` — interruptor único: `export const HOLDING_MODE_ACTIVE = false`. Cambiarlo a `true` sustituye **todas las rutas** por `HoldingPage`.
- `src/pages/HoldingPage.tsx` — la pantalla de espera en sí (revisar aquí dónde vive la fecha a actualizar — no confirmado en este handoff, falta leer el archivo).
- Precedente: se activó una vez el 2026-08-05 (commit `1d3091a`, "Fase HOLDING-PAGE-01") por un cambio de itinerario, y se desactivó el 2026-08-06 (commit `5cca801`) al reabrir con el itinerario nuevo.

**No confirmado por este handoff — primer paso al retomar:**
1. Leer `src/pages/HoldingPage.tsx` para ubicar dónde está hardcodeada (o no) la fecha que el usuario quiere cambiar.
2. Preguntar al usuario **qué nueva fecha** quiere mostrar y **por qué** se vuelve a pausar (no quedó dicho en la conversación cortada — evita asumirlo).
3. Confirmar si además del texto hay que tocar algo más (countdown, copy, etc.) antes de flipear `HOLDING_MODE_ACTIVE` a `true`.

## Recordatorios operativos activos (ya en la acta, resaltados aquí por su fecha límite)

- **2026-08-31** — los links de Clip vencen funcionalmente (fin de etapa `lanzamiento`
  en `pricingStage.ts`); pedir a Paulina los links de preventa antes de esa fecha y
  actualizar `src/config/clipLinks.ts`. Si la pausa que pide el usuario empuja fechas,
  vale la pena revisar si esto también se corre.
- Ningún pago se ha marcado como confirmado todavía (26 inscripciones en `pending`) —
  el circuito WhatsApp → marcado manual en InsForge sigue sin probarse con un pago real.

## Cómo retomar

1. Abre Claude Code apuntando a esta carpeta (`C:\vonde\enforma-sys\hybrid-registro`) —
   no se puede reabrir la sesión archivada directamente desde aquí, hay que iniciar una
   nueva sesión en esta carpeta.
2. Pide que lea `WORKSPACE_STATUS.md` completo y este archivo.
3. Retoma desde la pregunta de la pausa/lámina arriba.
